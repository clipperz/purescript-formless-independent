{
  "name": "purescript-formless-aj",
  "version": "0.1.0",
  "repository": "git@github.com:ajnsit/purescript-formless-independent",
  "author": "Anupam Jain <ajnsit@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "spago test",
    "build": "spago build",
	"clean": "while read gitIgnorePattern; do if [ \"${gitIgnorePattern}\" != \"/node_modules/\" ] && [ \"${gitIgnorePattern}\" != \"\" ]; then echo \"rm -rf .${gitIgnorePattern}\"; rm -rf .${gitIgnorePattern}; fi done < .gitignore",
    "generate-bower": "node generate-bower.js"
  },
  "devDependencies": {},
  "dependencies": {
    "purescript": "^0.14.4",
    "spago": "^0.20.3"
  }
}
